---
name: prd-forge
description: "Generate structured PRDs from requirements, design drafts, interaction specs, or meeting notes. Outputs .docx and Markdown with clarification prompts, screenshot manifest tracking, docx validation, and golden-test guardrails. Triggers: write PRD, generate PRD, requirements document, PRD Forge."
---

# PRD Forge

基于结构化 PRD 写作规范，从交互稿、设计稿、需求背景和会议纪要生成完整 PRD 文档。本阶段聚焦创建 PRD；增量更新工作流在 P4 阶段实现。

## Project Info

- Creator: ianzhao
- Repository: https://github.com/ianzhao001/PRD-Forge
- License: Apache-2.0

## 触发条件

当用户提出以下任一场景时使用本 skill：

- 上传交互稿/设计稿，要求写 PRD
- 提供需求背景，要求生成需求文档
- 明确提到“PRD”“需求文档”“产品需求文档”
- 需要将产品方案结构化输出为标准文档
- 上传会议纪要或评审记录，要求补充到新 PRD 初稿

## 强制纪律

本节是出稿纪律的完整定义。`references/prd_schema.md` 和 `references/writing_templates.md` 只保留摘要提醒，避免规则分叉。

1. PRD 是正式业务文档，不是生成过程记录。Word 正文、表格、修订记录、页眉页脚中不得出现任何生成来源痕迹、生成工具名或模型名。业务本身涉及智能能力时，必须表达为产品能力或系统能力。
2. Word 内不得出现本地绝对路径、文件协议地址或内部工程目录。设计稿和原型只写文件名、版本号或用户提供的线上链接。
3. 对外展示的模块、章节、图片标题必须是中文业务名；内部编号可用于脚本和临时沟通，但不得进入 Word 正文。
4. 修订人、作者、负责人只写业务负责人或 `/`；禁止编造负责人，禁止填写生成工具名。
5. 用户要求删除、合并、改名章节、模块或表格时，必须同步更新 Step 4、`references/prd_schema.md`、`references/writing_templates.md` 和生成脚本；交付前检索旧标题、旧模块名、旧表头。

## 核心工作流

### Step 0: 生成前确认

先判断输入是否已经覆盖以下问题。未覆盖时，在对话中合并成 2-5 个问题询问用户；不得为了凑齐元信息而编造。

| 必问项 | 默认处理 | 已覆盖判定 |
|--------|----------|------------|
| 模块边界 | 按交互稿一级业务模块识别 | 用户或纪要给出中文模块清单 |
| 高风险业务口径 | 仅从交互稿、HTML 逻辑和纪要推断 | 奖励、排序、结算、风控、开奖、实验等规则已写明 |
| 管理端范围 | 根据输入与风险推导 | 出现配置、阈值、开关、上下线、奖励档位等诉求时必含 |
| 实验范围 | 根据输入与风险推导 | 出现 A/B、分组、灰度、护栏指标等诉求时必含 |
| 埋点范围 | 根据输入与风险推导 | 出现关键交互或资金、积分流转时必含 |
| 负责人/作者 | 产品负责人；未知写 `/` | 用户、纪要或已有材料已提供 |

如果用户要求“直接生成初稿”，仍必须确认模块边界和高风险业务口径；其他非阻塞缺失项可写入待确认事项。

#### 出稿状态定义

以下三档是进入生成和交付前状态归类的唯一口径，Step 6 直接引用本定义。

| 状态 | 放行条件 | 未决事项处理 |
|------|----------|--------------|
| PRD 初稿 | 主流程可理解，模块边界和高风险口径已确认或明确标注假设 | 允许 `待补充`、`待确认`、`待设计补图`，但必须集中登记，且不能影响主流程理解 |
| 正式评审稿 | 不存在阻塞型未决事项；所有 P0/P1 与 L3 高风险模块闭环 | 资金、奖励、风控、结算、开奖、实验类模块必须包含 AC、异常路径和 owner |
| 正式归档稿 | 所有核心规则、截图、负责人、异常路径、管理端映射、埋点口径完成 | 未决事项应为 0；如有豁免，必须由用户明确说明 |

### Step 1: 收集输入

若用户明确要求先出 PRD 初稿，且已提供交互稿/设计稿与需求背景/会议纪要，可先生成一版；缺失项写 `/` 或 `待补充`，并在待确认事项中登记。

| 输入项 | 必需 | 说明 |
|--------|------|------|
| 需求背景 | 是 | 为什么要做，解决什么问题 |
| 交互稿/设计稿 | 是 | Figma 链接、截图、上传图片或 HTML 文件 |
| 功能名称 | 是 | PRD 标题 |
| 产品对接人 | 是 | PM 姓名或花名 |
| 设计对接人 | 推荐 | 设计师姓名或花名 |
| 协同方 | 推荐 | 团队与负责人 |
| 目标假设 | 推荐 | 验证什么假设 |
| 核心指标 | 推荐 | 期望影响的业务指标；未知不得编造数值 |
| 会议纪要/评审记录 | 推荐 | 提炼已确认改动、未决事项、owner 与风险 |
| 文档作者/修订人 | 推荐 | 默认产品负责人；未知写 `/` |

### Step 1.5: 输入归一化

不同输入形态先归一化，再进入交互稿分析。

- Figma 链接：记录文件名、版本号、线上链接；截图优先通过可用浏览器或设计导出能力按 2x/3x 设备像素比重新截取。
- HTML 交互稿：用浏览器渲染高清截图，按业务模块切分；同时解析 DOM、JS 条件分支、计算公式和渲染常量。
- 静态图片或截屏：仅作为展示素材；模块划分以用户说明、会议纪要或图片可见业务模块为准，不自行猜测字段来源。
- 会议纪要：先抽取已确认结论、未决事项、owner、风险四类条目，再写入对应章节。
- 多份输入冲突：优先级为用户明确说明 > 最新会议纪要 > 最新交互稿 > 旧文档。冲突影响业务规则时回到 Step 0 追问。

### Step 2.5: 截图清单生成

生成 PRD 前必须先生成或更新 `screenshot_manifest.yaml`，并与 PRD docx 放在同一目录。清单结构遵循 `references/screenshot_manifest.schema.yaml`。

- `modules` 列表必须与 PRD 第 4 章下的中文业务模块清单一一对应，不多不少。
- 每个模块必须登记 `module_name`、`screenshot_file`、`source`、`source_ref`、`device_pixel_ratio`、`missing_reason`、`owner`。
- 有截图模块的 `source` 只能为 `figma_url`、`html_dom`、`static_image` 或 `manual_screenshot`，且 `device_pixel_ratio` 必须不低于 2.0。
- 缺图模块的 `source` 必须为 `missing`，`screenshot_file`、`source_ref` 为空字符串，`device_pixel_ratio` 为 0，并填写 `missing_reason` 和 `owner`。
- 缺图模块必须在 PRD 展示列写 `待设计补图`，并在待确认事项中登记缺失原因和负责人；缺少登记时不得进入交付流程。

### Step 2: 分析交互稿

若无交互稿，跳过字段和截图提取；相关展示列写 `待设计补图`，并在待确认事项中登记缺失原因和负责人。

从交互稿中提取：

1. 页面、模块、组件结构。
2. 信息架构、字段、字段来源和计算口径。
3. 交互流程、状态变化、空态、加载态、异常态、降级态。
4. 可配置项，包括位置、数量、阈值、开关、模板、奖励档位。
5. HTML/JS 中会影响研发实现和测试的条件、公式、枚举、排序、过滤和状态判断。

如果同时提供会议纪要/评审记录，额外提取：

1. 已确认设计结论：哪些页面或模块要改、删、补、替换。
2. 未决事项：跨团队 owner、待确认规则、风控、消息、运营依赖。

### Step 3: 读取 Schema 和规范

按需读取以下参考文件：

- `references/prd_schema.md`：文档骨架和各章节字段说明。
- `references/format_spec.md`：标题、表格、图片、超链接和字段格式规范。
- `references/writing_templates.md`：需求详情写作模板、信息分级和反例。

### Step 4: 生成 PRD 内容

按 `references/prd_schema.md` 的章节顺序生成。PRD 的核心不是翻译设计稿，而是定义设计稿无法表达的业务逻辑；截图能表达的界面事实不展开复述。

#### 4.1 修订记录

- 创建首条：`今天日期 | v0.1 | 创建文档 | 产品对接人或 /`。

#### 4.2 背景、目标与策略

- 背景：提炼问题、机会和业务上下文。
- 目标：写成“验证假设：...”。
- 指标：区分定性指标和定量指标；未知数值写 `待补充`。
- 策略：提炼关键策略；有明确框架时再标注。
- 确认事项：已确认与待确认合并成一个表格。

#### 4.3 本期做和不做

- 本期做：只来自设计稿、会议纪要或用户确认。
- 本期不做：只来自会议边界、设计稿缺省或用户明确排除。
- 禁止编造产品方向或扩展需求。

#### 4.4 功能需求详情

- 按中文业务模块分节；章节标题不写负责人、团队或优先级。
- 每个模块使用三列表格：`模块 | 展示 | 需求描述`。
- 展示列嵌入最新设计稿真实高清截图；无法截图时写 `待设计补图`，并在待确认事项登记原因和负责人。
- 需求描述列使用多个段落，按 `业务目的`、`验收标准`、`业务规则`、`数据字段`、`异常路径` 组织；不得把多行内容塞进一个字符串。
- 需求详情只写设计稿看不出的逻辑。L0 不写，L1 弱写字段来源，L2 详写条件到行为，L3 补 AC、异常路径和 owner。
- HTML/JS 中的倒计时、投注计算、排序过滤、状态判断等必须提取为字段规则或条件到行为。
- 不写页面元素布局、颜色、间距、普通点击跳转；除非该交互影响业务规则、风控或验收。
- 完成后回扫配置诉求，凡涉及管理端、运营、后台、模板、阈值、奖励档位、上下线、开关等内容，必须映射到管理端需求。

#### 4.5 管理端需求

- 只写功能需求中明确需要后台或管理端支撑的能力，不扩展。
- 触发条件：输入中出现配置、阈值、开关、上下线、奖励档位、模板、运营配置等诉求。
- 无管理端配置时写 `/`，不写空泛能力。

#### 4.6 实验详情

- 触发条件：输入中出现 A/B、分组、灰度、实验周期、核心指标或护栏指标。
- 不满足触发条件且用户未要求时，写 `/` 或在 Step 0 追问。

#### 4.7 埋点信息

- 触发条件：输入中出现关键交互、转化链路、资金、积分、奖励、活动流转等内容。
- 页面埋点和后台埋点事件名使用中文；参数字段默认留空，等技术方案确认后补。

#### 4.8 名词解释

- 固定放在文档末尾。
- 只解释业务术语、行业术语和跨团队容易误解的概念。

### Step 5: 输出为 .docx 文件

优先使用 `python-docx`；若当前环境缺包、被拦截或图片/表格能力不足，可改用 Node.js `docx` 库。无论使用哪种方案，产物必须符合以下要求：

1. 正文标题使用 Word 原生 Heading 1-5，并带数字序号；封面主标题可使用自定义字号，正文标题不得靠字号和加粗伪装。
2. 表格使用浅灰边框，表头加粗；列宽按内容分配，需求描述列保留足够宽度，六列以上表格优先压缩序号、默认值、权限列。
3. 图片用真实文件字节嵌入；按页面宽度等比缩放，避免超高图撑满整页。
4. 每条 AC、规则、异常路径独立成段；Word 单元格中不得出现字面量换行标记或 HTML 换行标签。
5. Figma、飞书、iWork 等链接以超链接形式嵌入。

### Step 6: 交付

- 输出 .docx 文件到工作目录。
- 同时输出 Markdown 预览版本。
- 告知用户文档结构概览：章节数、表格数、图片数、待确认事项数。
- 按 Step 0 的三档定义标注交付状态：PRD 初稿、正式评审稿或正式归档稿。
- 生成单份 PRD 时，交付前只需针对该产物运行 validator：`python scripts/validate_prd.py <PRD.docx> [--strict] [--manifest screenshot_manifest.yaml]`。如果不在 skill 根目录运行，请把 `scripts/validate_prd.py` 替换为 PRD Forge skill 中 validator 脚本的实际路径。不要在 PRD 输出目录查找或运行 golden test。
- 交付前完成生成后自检；任一阻塞项未通过时不得作为正式评审稿或正式归档稿交付。

### Step 7: 降级与异常处理

| 异常场景 | 初稿处理 | 正式评审/归档处理 |
|----------|----------|-------------------|
| 设计稿无法截图 | 展示列写 `待设计补图`，登记缺失原因和负责人后继续 | 关键模块缺图需用户确认才可评审；归档稿原则上必须补齐或获得明确豁免 |
| HTML 解析失败 | 保留可确认的 L2/L3 规则，其余写 `待补充` | 影响高风险规则时必须追问 |
| Figma 链接无效 | 登记链接问题和负责人，继续初稿 | 正式稿需补有效设计来源或用户确认豁免 |
| 表格溢出 | 压缩短列，仍溢出则拆表 | 必须修复到页面宽度内 |
| 图片错位或超高 | 等比缩放，限制最大高度 | 必须修复 |
| 涉及资金、奖励、抽奖、开奖、结算、赔率 | 不得跳过异常路径、对账或风控说明 | 缺 AC、异常路径、owner 时不得交付正式稿 |
| 输入冲突 | 回到 Step 0 追问 | 未解决前不得进入正式稿 |

### Step 8: 增量更新占位

TODO(P4): 支持读取已有 PRD、追加修订记录、章节级 diff 合并和回归校验。P0 阶段不得假装已具备稳定增量更新能力；用户要求更新已有 PRD 时，可基于现有文档内容生成新版初稿，但需说明是人工合并式处理。

## 生成前自检

- Step 0 必问项已逐项判断，阻塞项已获用户确认。
- 输入已完成归一化，模块清单为中文业务名。
- 结构变更已同步到 `references/prd_schema.md` 和 `references/writing_templates.md`。
- 管理端、实验、埋点是否需要输出已按触发条件判断。

## 生成后自检

- Word 正文无生成来源痕迹、无生成工具名、无本地路径、无内部编号。
- 未生成独立文档元信息表；封面元信息只作为普通文本行出现。
- 所有正文标题为 Word Heading 1-5。
- 所有模块名、图片标题、章节名为中文业务名。
- 功能需求详情使用三列表格，展示列图片真实或标注 `待设计补图`。
- 每个 `待设计补图` 均已登记缺失原因和负责人。
- 功能详情中的管理端配置诉求已全部映射到管理端需求。
- L3 高风险模块包含 AC、异常路径和 owner。
- 表格单元格无字面量换行标记或 HTML 换行标签。
- 若用户删除、合并或改名旧结构，最终文档和 Markdown 预览中无旧标题残留。
- `screenshot_manifest.yaml` 与 PRD 第 4 章模块清单严格相等。
- 缺图模块已在 manifest 和 PRD 中同步标注，且 owner 与缺失原因已登记。

## Golden Test 门槛

Golden test 只用于验证本 skill 工程本身是否回归，不用于校验每一份新生成的 PRD。只有修改 validator、SKILL.md、reference/schema、模板或 PRD 输出规则后，才需要在 skill 根目录运行 `bash tests/golden/run_golden_tests.sh`，全部 PASS 才能交付 skill 变更。生成单份 PRD 时，应运行 Step 6 中的 validator 命令。

若 validator 行为需要主动变更，先改脚本，再跑出新报告并人工 review，确认符合设计意图后才能覆盖 baseline；禁止先改 baseline 再改脚本。
